<UserControl x:Class="Client.ManagerFolder.DataAnalysys.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.ManagerFolder.DataAnalysys"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="815"
             Background="#F5F7FA">
    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0056b3"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#A0A0A0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Фильтры -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Тип отчета:" FontSize="14" Foreground="#333333" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding ReportTypes}" SelectedItem="{Binding SelectedReportType, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ComboBoxStyle}" Width="200"/>
            <Button Content="Сгенерировать отчет" Style="{StaticResource ButtonStyle}" Command="{Binding GenerateReportCommand}"/>
        </StackPanel>

        <!-- Таблица результатов -->
        <ScrollViewer Grid.Row="1" MaxHeight="300" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding ReportResults}" AutoGenerateColumns="False" CanUserSortColumns="True" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Период" Binding="{Binding Period}" Width="*"/>
                    <DataGridTextColumn Header="Рентабельность капитала (%)" Binding="{Binding ReturnOnEquity, StringFormat={}{0:F2}}" Width="*"/>
                    <DataGridTextColumn Header="Долговая нагрузка" Binding="{Binding DebtToEquityRatio, StringFormat={}{0:F2}}" Width="*"/>
                    <DataGridTextColumn Header="Коэффициент автономии" Binding="{Binding AutonomyRatio, StringFormat={}{0:F2}}" Width="*"/>
                    <DataGridTextColumn Header="Изменение капитала" Binding="{Binding EquityChange, StringFormat={}{0:F2}}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <!-- Статус -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" FontSize="12" Foreground="#666666" Margin="0,10,0,0"/>
    </Grid>
</UserControl>